<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Keşfet"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/search_dark"
        android:layout_marginBottom="16dp"/>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="50dp"
        app:cardCornerRadius="25dp"
        app:cardElevation="2dp"
        android:layout_marginBottom="16dp">

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:hint="Etkinlik veya topluluk ara..."
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:drawablePadding="10dp"
            android:textSize="14sp" />
    </androidx.cardview.widget.CardView>

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayoutDiscover"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabMode="fixed"
        app:tabIndicatorColor="@color/search_green_main"
        app:tabSelectedTextColor="@color/search_green_main"
        app:tabTextColor="@color/search_dark"
        android:layout_marginBottom="8dp">

        <com.google.android.material.tabs.TabItem
            android:text="Tümü" />
        <com.google.android.material.tabs.TabItem
            android:text="Şehir" />
        <com.google.android.material.tabs.TabItem
            android:text="Topluluk" />
    </com.google.android.material.tabs.TabLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvDiscoverEvents"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:listitem="@layout/item_event_card" />

</LinearLayout>